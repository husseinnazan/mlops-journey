# mlops-journey
Learning log and projects on my path to MLOps engineering.

# MLOps Journey

Learning log for my path to MLOps engineering.

**Goal:** Backend/cloud engineer first, MLOps engineer after.

**Started:** August 2026

## Phase 1 — Backend foundations

- [x] Python fundamentals — syntax, functions (`notebooks/python/`)
- [x] SQL & relational databases (`notebooks/sql/`)
- [x] Git & GitHub (`notebooks/git/`)
- [x] Bash (`notebooks/bash/`)
- [x] Python Part 2 — dicts/tuples/sets, file I/O, try/except, *args/**kwargs (`notebooks/python/`)
- [x] OOP — classes, `__init__`, instance vs. class attributes (incl. attribute lookup/shadowing), inheritance (`super()`, overriding + extending a parent method). Composition deliberately scoped out — not needed for StudyMind. Applied to the `Car` example, then applied for real in the Student Grade Manager refactor below.
- [x] Type hints — annotations, function/parameter/return types, `List`/`Dict`/`Set`, custom types, `Any`, `Sequence`, `Tuple`, `Callable`, generics (Corey Schafer course), `mypy` static analysis, `Union`, and `Optional` corrected (means "can be X or None," not "argument is optional to pass" — that's controlled separately by a default value). Applied for real in the Student Grade Manager refactor below.
- [x] Decorators (+ closures) — closures and decorators videos watched (Corey Schafer). Notebook built (`notebooks/python/decorators-closures.md`) covering the `*args`/`**kwargs` wrapper skeleton, `functools.wraps`, decorators with arguments, and a retry-pattern preview. Core mechanism (`@decorator` rebinds the name to `wrapper`) worked out from first principles after a rough first attempt at the timer exercise. `@timer` applied for real to `StudentDB.get_all_students` and confirmed working against the live database. `@log_call` (replacing the old hand-written `log_action`) built and applied to `add_student`/`find_student`, using `func.__name__` for the log line — not yet confirmed against real `activity_log.txt` output. Exercises 3–5 (closure-based call counter, `@retry(max_attempts, delay)`, stacked decorators) left open.
- [x] Context managers — `with` as `try`/`finally` shorthand, `__enter__`/`__exit__`, state persisting via `self` (a `Timer` context manager built by hand, mirroring the `@timer` decorator's start/end timestamp logic), and the exception arguments (`exc_type`/`exc_value`/`traceback`, and why they're `None` on success). `contextlib.contextmanager` + `yield` shown as a shortcut and understood conceptually (same before/after split as a decorator, `yield` standing in for `func(*args, **kwargs)`) but deliberately not adopted — sticking with the explicit class-based form going forward. Taught entirely through throwaway examples (`Locker`, `Party`, `Timer`), not applied to Student Grade Manager — see note below.
- [x] JSON — `json.dumps`/`json.loads` vs. `json.dump`/`json.load` (string vs. file), the Python↔JSON type mapping (`True`→`true`, `None`→`null`, tuples flattening to lists, dict keys always becoming strings), and nested dict/list access. Taught via a "shared recipe card between kitchens" analogy, then checked against a quiz — 4 of 5 questions answered and corrected, nested-access exercise (#5) never got a final confirmed answer. Notebook at `notebooks/python/json-basics.md`; formal exercises 1–5 not run.
- [x] HTTP requests (+ Wikipedia search script using `requests`) — `requests.get()`, the `Response` object vs. `.json()`, query params, `timeout`, and `try`/`except requests.exceptions.RequestException` walked through conceptually, including a full Wikipedia opensearch example. Checked off on understanding rather than hands-on execution — the 5 practice exercises (including the roadmap's Wikipedia search script) were solved and explained rather than run. Worth actually running `search_wikipedia()` for real at some point, but not blocking progress on it right now.
- [x] asyncio & requests vs. aiohttp *(last standalone topic, right before FastAPI)*
- [x] FastAPI *(official tutorial — learned hands-on while building Codemap below, not a separate pre-read)*
- [x] Codemap — FastAPI + SQLite *(capstone — final item, uses everything above except LLM integration, which was intentionally descoped; see note below)*

**Note:** Custom exception handling dropped as a dedicated Phase 1 topic — basic `try/except`/`OSError` handling is already covered (Python Part 2, Student Grade Manager). Foundational DSA (arrays, hash maps, linked lists, stacks/queues, trees, recursion) postponed out of Phase 1 entirely — moved to a bridge topic right before Phase 1.5's daily NeetCode/Grind75 grind starts, so it's learned right when it's needed instead of sitting unused. SQL window functions dropped from the roadmap entirely — not deferred, just not needed for what's ahead. LLM API integration (summarization, flashcard generation) was removed from the Phase 1 capstone. Building it now would duplicate Phase 4's dedicated LLM API basics and RAG work. The capstone instead covers Notes, Tasks, and Pomodoro session tracking — full CRUD across three real features — built with FastAPI and SQLite and pushed to its own standalone repository, StudyCorner.

## Projects

- [Student Grade Manager (CLI)](projects/student-grade-manager) — CLI app with SQLite persistence. Refactored `db.py`'s loose functions into a `StudentDB` class wrapping the connection/cursor as instance attributes, with a `Student` class replacing raw tuple access (`student.name`/`student.score` instead of `student[1]`/`student[2]`) across `main.py`. Both files fully type-hinted, including `Optional[Student]` on `find_student`. `@timer` and `@log_call` decorators added and applied. **Retired as the practice vehicle for new topics as of 2026-08-11** — remaining Phase 1 topics will use standalone/throwaway examples instead of building further onto this project.
- [Bash Todo App (CLI)](projects/bash-todo-app) — `add` / `list` / `remove` todo list in pure Bash, backed by SQLite at `~/.config/todo-app/db.sqlite`. Built to learn `case` branching and calling `sqlite3` from a shell script.
- [StudyCorner](https://github.com/husseinnazan/StudyCorner) — Phase 1 capstone. FastAPI + SQLite REST API for Notes, Tasks, and Pomodoro session tracking. Standalone repository, separate from this learning log.

## Log
- **2026-08-06** — Set up environment (Git, VS Code, Python). Started SQL.
- **2026-08-06** — Learned SQL/relational databases (SELECT, joins, aggregates, subqueries, PK/FK, many-to-many junction tables) via DBeaver. Added `notebooks/sql/`.
- **2026-08-07** — Converted Student Grade Manager CLI from in-memory storage to SQLite. All 5 menu options now backed by real queries (INSERT, SELECT, aggregates, WHERE filtering).
- **2026-08-08** — Refactored Student Grade Manager into db.py (data layer, returns data) and main.py (application layer, handles input/display). Kept the original cli-app.py for comparison.
- **2026-08-08** — Watched Git & GitHub crash course (branching, merging, conflicts, stash, revert, rebase, PRs). Added `notebooks/git/git-github-fundamentals.md`.
- **2026-08-09** — First contact with Bash: script files, variables, positional arguments (`$1`), `if`/`else` with `-z`/`-d` tests, `for` loops over lists and globs, exit codes (`$?`, `&&`, `||`, `exit`), redirection (`>`, `>>`), and pipes (`|`). Added `notebooks/bash/bash-fundamentals.md`.
- **2026-08-09** — Built a Bash Todo App from scratch: `case`-based command routing (`add`/`list`/`remove`), a SQLite table created via `sqlite3` calls from the script, and safe variable quoting (`'$2'` inside an `INSERT` vs bare `$2` in a numeric `WHERE`). Added `projects/bash-todo-app`.
- **2026-08-10** — Python Part 2: dictionaries, tuples, sets, file I/O (`with open`, read/write/append modes), `try/except` (multiple excepts, `else`/`finally`, raising custom errors, `OSError` for file failures), and `*args`/`**kwargs`. Applied all four directly to the Student Grade Manager CLI — added `export_students_to_file()` and `log_action()` to `main.py`. Added `notebooks/python/python-part-2.md`.
- **2026-08-10** — Roadmap update: tied closures explicitly into the Decorators topic (a decorator is a closure that returns a wrapped function), added a small Wikipedia-search script (using `requests`) as the practice project right after HTTP requests, marked Unit testing as optional if the 6-week window runs tight, and added `asyncio` + `requests` vs. `aiohttp` as a topic right before FastAPI, since FastAPI is async-first.
- **2026-08-10** — OOP, first pass: built a `Car` class (`__init__`, instance attributes, a class attribute `office_costs`) and self-assessed it against the roadmap. Found inheritance and composition were missing, plus the instance-vs-class attribute distinction was never actually tested. Taught: attribute lookup order (instance shadows class, never overwrites it), inheritance (`super().__init__()`, overriding + extending a parent method), and composition (has-a vs. is-a) via standalone `Animal`/`Dog` and `Engine`/`Vehicle` examples.
- **2026-08-10** — Roadmap restructure: OOP scoped down to drop composition, keeping only inheritance — checked off. Foundational DSA pulled out of Phase 1 entirely, moved to a bridge topic immediately before Phase 1.5's daily grind starts, so it's taught right when needed rather than sitting unused for weeks. Custom exception handling dropped as a dedicated topic — basic `try/except`/`OSError` (already covered) is enough; StudyMind's retry decorator will catch standard exceptions instead of a custom hierarchy. New Phase 1 order: Git → Bash → OOP → Type hints → Decorators → Context managers → JSON → HTTP requests → asyncio → FastAPI → capstone. Starting Type hints today.
- **2026-08-10** — Type hints, first pass (Tech With Tim video): annotations, function signatures, `mypy`, `List`/`Dict`/`Set`, custom types, `Optional`, `Any`, `Sequence`, `Tuple`, `Callable`, generics. Two gaps found on review: `Union` wasn't covered at all, and the video's explanation of `Optional` was actually wrong (presented it as "argument doesn't have to be passed," which several top comments and the official docs correct — `Optional[X]` means `Union[X, None]`, i.e. the value can be `None`, which is a completely separate concept from an argument having a default).
- **2026-08-10** — Type hints, closed out (Corey Schafer course): confirmed Union and correct Optional were the only two gaps from the first pass. Union taught directly — `Union[X, Y]` means a fixed set of allowed types (not "any type," that's `Any`), with `X | Y` as the Python 3.10+ shorthand. Optional's real meaning locked in against `find_student`'s actual behavior (returns a tuple or `None`) as the concrete example. Type hints now checked off.
- **2026-08-10** — Student Grade Manager refactored: `db.py`'s free functions rebuilt as a `StudentDB` class (connection/cursor moved to `self.conn`/`self.cursor`, created once in `__init__`), and a new `Student` class introduced so rows come back as objects instead of raw tuples. `get_all_students`/`get_passing_students` now return `List[Student]`, `find_student` returns `Optional[Student]` (the real payoff of `Optional` — reflects that it genuinely returns `None` on no match), `get_stats` returns `Tuple[Optional[int], Optional[int], Optional[float]]` (the row itself always exists, but its values can be `None` on an empty table). `main.py` updated to instantiate `StudentDB()` once and use `student.name`/`student.score` throughout instead of tuple indices. Both files fully type-hinted. OOP and type hints checked off Phase 1 — end-to-end test run still pending.
- **2026-08-11** — Decorators & closures: watched Corey Schafer's Closures and Decorators videos, notebook built covering the wrapper skeleton, `functools.wraps`, decorators with arguments, and a retry-pattern preview (`notebooks/python/decorators-closures.md`). First attempt at the `@timer` exercise hit real friction — forgetting to pass `*args`/`**kwargs` through to the wrapped function, not capturing/returning the wrapped function's result, and a broken `print` call. Worked through it by tracing the simpler `@shout`/`hi()` example step by step: confirmed that `@decorator` rebinds the function's name to `wrapper`. `@timer` applied to `StudentDB.get_all_students` and confirmed working against the real database. `@log_call` built (replacing `log_action`) and applied to `add_student`/`find_student`. Decorators checked off Phase 1.
- **2026-08-11** — Context managers: taught from scratch via a locker/gym analogy (`try`/`finally` guarantee, `with` as shorthand for it), then `__enter__`/`__exit__` mechanics built up slowly — what each returns, when each runs, and the three exception arguments (`None` on success, filled in on failure). Practiced with throwaway `Locker`, `Party`, and `Timer` classes rather than the real project — decided to retire Student Grade Manager as the practice vehicle for new topics going forward. `contextlib.contextmanager` shown as a shortcut (mapped directly onto the `@timer` decorator's before/`yield`/after structure) but consciously not adopted in favor of the explicit class-based form. Context managers checked off Phase 1.
- **2026-08-11** — Pace check-in: after decorators and context managers back-to-back in one sitting, paused to explicitly verify understanding rather than just feeling uncertain about it — confirmed both topics were genuinely solid (correct output predictions, a `Timer` context manager written independently, `@timer` working on real code), and noted 8 topics done in 9 days against a 6-week Phase 1 estimate, meaning there's real schedule slack. Took a break rather than pushing straight into new material.
- **2026-08-11** — JSON: baseline quiz first (mixed with an HTTP baseline, done cold before either topic was formally taught — better than expected on both). JSON then taught from zero via a "shared recipe card between different kitchens" analogy — the type-mapping quirks (tuples→lists, int keys→string keys) landed correctly on the second pass. HTTP requests walked through conceptually afterward: `requests.get()`, the `Response` object vs. `.json()`, status codes, `params`, `timeout`, and error handling with `requests.exceptions.RequestException`, plus a full Wikipedia opensearch walkthrough. Checked off on conceptual understanding — genuinely tired by this point in the day, chose to move forward rather than push through hands-on exercises. Running the Wikipedia search script for real is still a good idea whenever there's energy for it, just not gating progress.
- **2026-08-11** — learned asyncio , aiohttp .
- **2026-08-17** — Codemap capstone finalized as StudyCorner: Notes, Tasks, and Pomodoro session CRUD, all built with FastAPI and SQLite. Diagnosed and fixed a `sqlite3.ProgrammingError` from SQLite's default same-thread restriction (`check_same_thread=False`). LLM API integration deliberately descoped from the capstone since it duplicates Phase 4's dedicated LLM API basics and RAG work; roadmap's capstone description updated to reflect this. Pushed to a standalone repository (github.com/husseinnazan/StudyCorner) with its own README, license, and changelog.